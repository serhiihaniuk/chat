/**
 * Rich, page-only explanations for each glossary term (2-4 sentences).
 * The Vocabulary page renders these; the short `definition` in glossary.ts
 * stays for the inline hover cards. Keyed by glossary term id.
 */
export const glossaryExplanations: Record<string, string> = {
  "llm": "A large language model is a function that takes text in and predicts text out, and that is the whole job. It has no memory between calls, like a brilliant consultant with total amnesia who forgets everything the instant a call ends. Because of that, the server must resend the entire conversation on every turn; the message list it assembles is the model's whole universe for one call.",
  "token": "The model does not read letters or whole words, it reads tokens, which are word-fragments. \"Jira\" might be one token while \"tickets\" might be two. This matters because usage is metered in tokens: input tokens are everything the server sent up, output tokens are what the model wrote back, and a context window is sized in tokens too.",
  "context": "Everything assembled into a single model call: the system instructions, the past messages, the current question, and the descriptions of available tools. It is the entire case file you hand the amnesiac consultant, because the model knows nothing beyond what is in front of it. All of it competes for one fixed token budget, which is why a dedicated preparation step decides what makes the cut.",
  "context-window": "The maximum number of tokens a model can take in one call, a hard ceiling on how thick the case file can be. Everything you send up, the system instructions, the history, the question, and the tool descriptions, competes for that fixed budget. When the material is too big to fit, something has to be dropped, which is the whole reason context preparation exists as a budget-admission step.",
  "prompt": "Not one string but a list of role-tagged messages handed to the model in a single call. The server owns nearly all of it, the standing instructions and the full history, while the browser contributes only the raw content of the new message. Treating the prompt as a structured list rather than a blob is what lets the server keep control of who said what.",
  "message": "One item in the model's input list, each tagged with a role so the model knows who is speaking. A message is the unit the model reads and the unit the conversation is stored in. The browser sends only the content of a new message; the server attaches the role and slots it into the list.",
  "role": "A tag on each message telling the model who said what, either system, user, or assistant, like character names in the margin of a script. This is a security boundary, not a formatting nicety: the model trusts the system role, so if the browser could set roles it could smuggle in instructions the model would obey. The browser therefore never picks a role; the server labels untrusted browser text as user and writes the system role itself.",
  "delta": "One fragment of new text streamed the moment the model produces it, carried as a sidechat.delta event. Rather than make someone stare at a blank panel for several seconds, the server sends the answer in small pieces, like a ticker tape printing a word at a time instead of mailing the whole letter at the end. The browser appends each fragment onto the visible message as it arrives.",
  "streaming-sse": "Streaming means sending the answer in pieces as the model writes it, instead of waiting for the whole reply. The transport here is SSE, Server-Sent Events, a one-way stream of text frames over a single HTTP response with one event per frame. Each frame carries a sequence number that must always increase, so a delayed or duplicated frame is caught, and that same number doubles as a resume cursor when a browser reconnects.",
  "tool": "A capability the model can request but cannot run itself, also called function calling. A bare model can only produce text; a tool is how it reaches Jira, a database, or an API, but the critical part is that the model only asks. It writes a work order naming the tool and its arguments; your code runs the tool and hands the result back for the model to read on its next pass.",
  "tool-loop": "The back-and-forth that turns a single request into real work: the model emits a tool call, the runtime executes it and feeds the result back in, and the model reads that result and may call another tool or write the final answer. Think of a manager who writes work orders but has no hands while an assistant does the doing and returns the folder. Each result becomes fresh input the model reads on the next pass, which is the stateless-replay idea showing up again.",
  "agentic": "Describes a system that can do more than chat: it takes actions by calling tools, observes the results, and decides what to do next, looping until the task is done. The key signal is that the model chooses the steps. A request like \"show me my open tickets\" is agentic when the assistant decides on its own to search, then uses what it found, without ever being told to call a tool.",
  "provider": "The company or API you call to reach a model, like an airline that operates the flights. One provider offers many models, so the provider is the account and endpoint while the model is the specific brain. This codebase ships exactly one real provider, openai, plus a fake provider used in tests.",
  "model": "One specific brain a provider offers, such as gpt-5.4-mini, where the provider is the airline and the model is the particular aircraft. The browser never names a model; the server decides, and which model is allowed for a given turn is fixed by the assistant profile's policy rather than by the request. Internally the provider and model are paired together so a turn always runs on a known, allowed combination.",
  "reasoning": "Some models can think privately before answering, working through a scratchpad of deliberation that is hidden by default, like a chess player calculating moves silently before touching a piece. The reader sees the final answer, not the working. Here reasoning is a provider-level effort setting, not something the browser controls or normally sees.",
  "system-prompt": "The server's standing instructions to the model, delivered under the trusted system role, things like the assistant's tone and the rules it must follow. Because the model obeys whatever carries the system role, this text is written by the server and never by the browser. Keeping the system prompt server-owned is what stops untrusted page content from rewriting the assistant's rules.",
  "turn": "One complete cycle: a user message goes in and one finished assistant reply comes out, including any tool loops that happened in between. A turn is not the same as one model call, since a single turn may make several calls while the tool loop runs. The system records each turn as a durable row carrying the model and policy that governed it, which is why turn is a first-class noun throughout the codebase.",
  "hallucination": "Model output that sounds fluent and confident but is false or unsupported, because the model is predicting plausible text, not consulting a source of truth. It is the failure mode that makes a bare model risky for real questions. Tools and grounding reduce it by handing the model actual data to work from instead of leaving it to guess.",
  "side-chat": "The assistant foundation this repo ships so an enterprise can drop a chat experience into a product it already owns, rather than build one from scratch. It is not a finished app or a demo: it supplies the widget, the browser protocol, the deployable service, the runtime boundary, and persistence, while the consuming app keeps owning its UI, auth, and data. Think of it as an engine you mount, not a car you drive off the lot.",
  "host-app": "The consuming web application that embeds Side Chat and keeps full ownership of its own UI, authentication, data, permissions, and business logic. Side Chat is the guest here; the host stays in charge of who the user is and what they may see, and merely hands the assistant a verified scope to work inside. This matters because Side Chat trusts nothing the host page merely claims, only authority the host has actually proven.",
  "embedding-surface": "The specific place inside the host application where the widget is mounted, such as a customer portal, an admin dashboard, or an internal back-office tool. It is a product-shape distinction rather than a separate code type: the same host app can expose the assistant on several surfaces. The handle in code is the host app id, so think of the surface as the room and the host app as the building it sits in.",
  "conversation": "A durable chat thread that ties together a run of user messages and the assistant turns that answered them. It is the unit that survives reloads and reconnects, so a user can leave and come back to the same thread, and it is the scope a title is generated for. Unlike a single turn, which is one exchange, the conversation is the whole transcript those exchanges accumulate into.",
  "conversation-title": "A short display label generated once for a thread, just after its first successful exchange, and only when configuration turns the feature on. It exists purely to make a list of past chats scannable, so it is enrichment rather than part of answering: if title generation fails, the turn still succeeds and the failure is merely observed. A small backstage model writes it from the first user message and reply, in two to six plain words.",
  "auxiliary-model-job": "A model task the service runs on the side, outside the main chat turn, with conversation-title generation being the one job shipped today. These jobs are configured by the deployment rather than requested by the browser, and they never block or alter the user-facing answer. Picture the main turn as the kitchen serving your order while an auxiliary job is the staff tidying up and labelling things afterward.",
  "model-catalog": "The backend-published list of which provider and model ids are available, their human display names, and which reasoning efforts each one allows. The widget reads it to populate the model picker, so it is the menu of legal choices the server is willing to honor. Because the server owns this list, the browser can only pick from what the catalog advertises, never name an arbitrary model of its own.",
  "model-preference": "An optional, per-turn request from the browser asking that this one turn use a particular model. It is a wish, not a command: the service validates it against the catalog and policy before honoring it, and an unknown or disallowed choice is rejected rather than silently obeyed. This keeps model selection server-authoritative while still letting a user steer a single answer.",
  "reasoning-effort": "A per-turn dial, expressed in provider-neutral terms, that controls how much private thinking the model does before it answers. The same six levels (none, minimal, low, medium, high, xhigh) are mirrored across the protocol, runtime, and domain layers, and each provider adapter decides how to translate the chosen level into its own SDK options. It lets a turn trade speed for depth without the browser ever touching a provider-specific knob.",
  "prepared-context": "The complete, finished snapshot of everything one turn will feed the model, assembled by core before the runtime ever executes. It bundles the resolved profile and policy, the admitted history, the scored candidates, the rendered context board, and the final role-tagged runtime messages. It is the line in the sand the architecture insists on: the runtime receives this package as-is and must not reach back out for more host data or history.",
  "context-candidate": "One individual piece of possible context, such as a slice of conversation history or a chunk of host-supplied data, that has been scored and classified but not yet admitted. Each carries its provenance, trust level, redaction class, and an estimated token cost, which is exactly what the admission selector weighs when deciding whether to keep or drop it. A candidate's text becomes visible to the model only after it survives that selection.",
  "context-admission-selection-mode": "A record of how the selector actually behaved when fitting candidates into the budget for one turn. In include_all mode it admits everything and merely notes the budgets it would have used, whereas in budgeted mode it may genuinely drop candidates to stay within the token limit. The distinction matters for audit: a recorded budget proves trimming only when this mode says the selector was budgeted.",
  "system-prompt-id": "A durable, stable identifier naming where a turn's resolved instructions came from, tied to a profile rather than to the prompt text itself. Because the actual wording can be edited over time, the id is what persistence and audit trails point at, giving a fixed label for a changeable source. It answers which profile spoke, not what it said.",
  "system-instructions": "The resolved standing-instruction text that core renders into the model's system-role message for one turn, drawn from the selected turn profile. This is the server's voice telling the model how to behave, and it is always written server-side, never by the browser. It is the concrete content behind a system prompt id: the id names the source, while these are the actual words handed to the model.",
  "user-message": "The text a person actually typed and submitted, stored and shown back to them with the role 'user'. The server assigns that role itself the moment it persists the message — the browser never gets to claim it was the user, the system, or the assistant. It is the trigger that starts a turn, and it is deliberately a narrower idea than 'input' or 'prompt', which sweep in the system instructions and history the browser does not control.",
  "assistant-turn": "One full assistant reply to one user message, from the first setup step through the final terminal event, including every tool call and model round-trip in between. The key thing it is not is a single model call: a turn that searches Jira, reads the result, and then writes prose is one turn but several calls to the provider. It is the unit everything else hangs off of — it has its own durable record, its own id, and its own resumable event stream.",
  "stream-chat-turn": "The product-level workflow in core that drives a single assistant turn: it resolves the plan, runs the safety guards, prepares the context, then maps and streams the generated events out. Think of it as the recipe for a turn, written once and independent of HTTP or any provider. Keeping this logic in one workflow is what lets the service layer stay thin and the runtime stay swappable.",
  "turn-policy-decision": "The resolved set of rules that govern a single turn: which assistant profile applies, which model and reasoning effort are allowed, which tools and commands may run, what needs approval, which executor runs it, and the system instructions. It is decided once, up front, from the host's capability manifest — not negotiated by the browser's request. This is the choke point that makes a turn safe by construction: the model can only ever reach the tools and instructions this decision permitted.",
  "resolved-turn-plan": "The bundle the workflow produces after deciding how a turn will run: the capability manifest, a hash of that manifest, the TurnPolicyDecision, and the fully resolved profile, all in one value. It is the frozen, ready-to-execute snapshot handed from planning to generation, so nothing downstream has to re-derive what was already settled. The manifest hash lets the system prove later exactly which capabilities were in force when the turn ran.",
  "server-owned-generation": "The principle that the turn is produced on a background fiber owned by the service, forked off the original request, rather than inside the HTTP response the browser is holding. Because generation lives on that detached fiber, the answer keeps being produced even if the user reloads the page, closes the panel, or loses their network. This single decision is what makes the whole turn resumable: the browser is only ever a watcher, never the thing doing the work.",
  "turn-runner": "The per-instance component that actually forks each turn's generation onto a fiber and keeps a registry — a FiberMap keyed by assistantTurnId — of every turn currently running on this server. It is how one instance knows which live turns it owns, so it can interrupt the right fiber on a cancel and tear them all down cleanly on shutdown. It only forks generation for a genuinely new turn, so a retried request rejoins the existing one instead of spawning a duplicate.",
  "durable-turn-event-log": "The append-only, per-turn ordered list of events that is the single source of truth for what happened in a turn — every delta, activity, and terminal, written to the turn_events table in sequence. Because the durable log, not the live connection, is authoritative, a dropped browser can reconnect and replay exactly what it missed and nothing is lost. The browser only ever subscribes to this log; it never writes to it, which is what keeps a turn's history honest across crashes and reconnects.",
  "replay-offset": "The cursor a subscriber passes when opening the stream — after=<seq> — telling the server to send only events past that sequence number. A brand-new viewer asks from the start and catches up from the beginning; a reconnecting one passes the last sequence it saw and resumes precisely from there, with the 'started' event always sitting at sequence 0. This is the mechanism that turns reconnection from a special case into the ordinary path: rejoining a turn is just the same request with a higher offset.",
  "owner-lease": "A compare-and-set claim that binds one running turn to exactly one server instance, stamped with a lease epoch so ownership can be proven and revoked. The owner renews it on a heartbeat; if a renewal matches no row — because a reaper or a new owner bumped the epoch — the owner knows it has been fenced and stops writing. This is what guarantees that even across restarts and deploys, only one instance ever appends a given turn's events, so the log can never be corrupted by two writers.",
  "reaper": "A background sweep that finds turns still marked 'running' whose owner lease has expired and writes a terminal event to close them out. It is the safety net for a server that crashed mid-turn: without it, a dropped owner would leave a turn 'running' forever with nothing to finish it. It fences the dead owner by bumping the lease epoch and records an honest outcome — a user-requested cancel as aborted, otherwise a timeout — picking rows with locking that keeps concurrent sweeps from stepping on each other.",
  "pruner": "A background sweep that deletes the event rows of old, already-finished turns once they pass the configured retention window, reclaiming storage. Crucially it removes only the streamed events, not the turn record or its final message, so the conversation still reads correctly afterward — the turn still resolves. The trade-off is that a pruned turn can no longer be replayed from an offset, which is the exact situation the replay_expired error exists to signal.",
  "pre-start-failure": "Anything that goes wrong before the 'started' event is emitted — bad input, a failed authority check, a rejected plan, a guard that blocks the turn. Because the stream has not opened yet, these are returned to the caller as an ordinary JSON error with an HTTP status, and in most cases no turn is created at all, so nothing half-started leaks out. The 'started' event is the fence: a failure on this side of it is a clean rejection the browser can simply retry.",
  "post-start-failure": "Anything that goes wrong after the 'started' event has been emitted and the stream is already live — a provider error, an interruption, a safety stop mid-generation. There is no caller to reject anymore, so the failure is recorded as a terminal event appended to the log: a sidechat.error, or a sidechat.blocked for a safety stop. The browser learns the outcome by reading that terminal off the stream, exactly the same way it would learn of success, so every started turn ends with a definite, durable result.",
  "ai-sdk-stream-part": "The raw, lowest-level event the AI SDK emits while talking to a provider, such as a text fragment or a tool-call part. It is the first of three event vocabularies and never leaves the agent-runtime package: the moment it crosses a boundary it is rewritten into a RuntimeEvent. Keeping it sealed inside one package is the whole point, so a provider quirk or a leaked SDK shape can never reach the browser unchanged.",
  "runtime-event": "The normalized, provider-neutral event the agent runtime hands up to core, the middle of the three event vocabularies. The kinds are started, output_delta, activity, completed, error, and blocked, each stamped with requestId, assistantTurnId, and an internal sequence. Think of it as a translation layer: whatever the SDK actually said gets flattened into this fixed enum, so core works against one stable contract instead of a moving provider API.",
  "runtime-activity-details": "The provider-neutral payload carried on a runtime activity event, holding the optional extras for one visible work row: sources, images, and tool input or result. Tool exceptions are reduced to a plain errorCode here, so no thrown value or raw stack ever travels with the event. Core later maps this into the browser-safe activity detail; it is the runtime-side half of how a Searching Jira row gets its links and results.",
  "map-runtime-event": "The single core function that translates one RuntimeEvent into its browser-facing sidechat.v1 event (or none). It is where the second-to-third vocabulary hop happens, and it carries the surprising rules: runtime.started is dropped because core emits its own sidechat.started, and the browser sequence is renumbered fresh from zero so internal runtime numbers never leak. This one function is the gate that guarantees the browser only ever sees the public protocol, never the runtime contract.",
  "sidechat-stream-event": "Any event in the browser-facing sidechat.v1 vocabulary, the last of the three and the only one a client is ever allowed to receive. The members are started, delta, activity, completed, error, blocked, and history, each tagged with protocolVersion, eventId, and a sequence. It is the public contract for one turn's stream, and the widget re-validates every frame against it on arrival rather than trusting the sender.",
  "activity-event": "A row describing the model's visible work inside one turn, such as a progress note, a reasoning summary, a tool call, or a host command. Events sharing one activityId update the same row in place rather than stacking new ones, which is how a Searching Jira line flips from running to completed with results. It lets the user watch the assistant think and act, while the actual tool input and output stay tucked inside the row instead of becoming separate chat messages.",
  "turn-activity-event": "A lightweight cross-conversation signal that says a turn is generating somewhere, delivered on its own GET /chat/activity stream rather than on a single turn's stream. It is what powers the little generating dot on chats you are not currently looking at. Despite sharing the word activity, it is a completely separate thing from an in-turn activity event: different stream, different codec, no sequence and no terminal, and the two must never be conflated.",
  "terminal-event": "The one final event that closes a turn's stream and settles its state, always exactly one of completed, error, or blocked. Every stream is guaranteed to end with one and only one of these, with nothing allowed after it. It is the full stop on the turn: the widget knows the answer is done, failed, or was stopped, and a stream that ends without one is treated as broken.",
  "sidechat-blocked": "A terminal safety stop, emitted when a turn is halted before producing a usable answer, for example because a content filter tripped. It is deliberately kept distinct from completed so a refusal is never mistaken for a real reply, carrying a public reason and a fixed safe message instead of model output. Treat it as the third way a turn can end: not finished, not a crash, but intentionally stopped.",
  "sidechat-history": "A replay payload carrying past messages rather than live streamed text, sent on initial load or as a fallback after a stream can no longer be resumed. When the durable log has been pruned and replay_expired comes back, the widget asks for history to rebuild the conversation from saved messages. It has no runtime equivalent and exists purely so the browser can recover a readable thread when live replay is impossible.",
  "tool-call-result-error": "The three faces of one tool invocation on an activity row: the model's request to run a tool with its arguments, the successful result it produced, and the public failed shape if it threw. Crucially the failure is reduced to a stable errorCode, never a raw exception or stack trace, so the browser learns a tool failed without seeing anything sensitive. All three share one toolCallId, so they update a single row as the call progresses from asked to answered.",
  "host-command-result": "A command Side Chat sends out to a host capability, such as navigate or open a record, paired with the structured result the host hands back. The result reports a status (applied, rejected, unsupported, failed, or timed out) plus a code and optional data, so Side Chat knows exactly how the host responded. It is the seam where the assistant reaches out and acts on the surrounding app, rather than just producing text.",
  "widget-message-activity": "The client-side state the widget builds up by folding incoming protocol events into something renderable: the chat messages on one side and the timeline of activity rows on the other. The browser never stores raw events; it reduces them into this view model, appending delta text to a message and updating activity rows by id. It is the last stop in the chain, turning the validated stream into the bubbles and step rows a person actually sees.",
  "protocol-error-code": "A stable, public code naming why a turn failed, carried on a sidechat.v1 error event after the stream has already opened, for example provider_failed. It is a curated set that deliberately hides provider specifics, with unknown runtime failures collapsed down to provider_failed so no internal detail leaks. Think of it as the in-stream cause of death for a turn that started but could not finish cleanly.",
  "transport-error-code": "A code explaining why a stream could not even open, returned as a plain JSON response before any SSE frame is sent. It is kept strictly separate from protocol error codes, which only apply once the stream is live: this one is about the connection failing at the door rather than the turn failing mid-flight. The distinction matters because the two are surfaced and recovered differently.",
  "replay-expired": "The single transport error code, returned as an HTTP 404, meaning the durable log for a turn has been pruned and can no longer be replayed from the requested point. It is what a browser hits when it tries to resume a stream whose events have aged out of retention. Rather than erroring out, the widget responds by falling back to sidechat.history to reload the conversation from saved messages.",
  "auth-context": "The proven authority for one request, carrying the tenant, workspace, acting subject, roles, and scopes that some trusted source has already vouched for. Every protected step gates on it: no AuthContext, no persistence, no private context, no model call. Think of it as a verified badge that says not just who you are but exactly which doors you may open today; the system trusts the badge precisely because it was issued by something it already trusts, never by the visitor.",
  "tenant": "The outermost authorization boundary, sitting one level above workspace, usually a whole customer organization. It is a real, load-bearing id that authority compares first, so a request that proves rights in one tenant can never reach into another's data. Crossing it is treated as a hard rejection rather than an edge case: two companies share the same running service but live in sealed rooms, and the tenant id is the lock on each door.",
  "workspace": "A tenant's specific authorized scope for a request, written as the pair of a tenant id and a workspace id together. Conversations and activity belong to a workspace, and a request must prove it may act in exactly that one before any work happens; a mismatch is denied as a cross-tenant or cross-workspace error. It is the difference between belonging to a company and being allowed into one particular team's room within it.",
  "subject": "The principal actually doing the work on a request, identified by a subject id paired with the underlying user id. It answers who is acting right now, which is why the cross-conversation activity feed is scoped per workspace and subject so people only ever see their own in-progress turns. Where the user is the person, the subject is the badge they are currently acting under, and authority decisions and audit trails both hang off it.",
  "user": "The human being behind a subject: the durable person identity, separate from the role or session they happen to be acting through. Keeping it distinct from the subject means the same person can be represented consistently even as the way they connect changes. It is the name on the ID card rather than the visitor badge clipped over it for today's visit.",
  "authority-denial": "The fail-closed answer the system gives when a request cannot prove it is allowed, carrying a precise reason code such as missing_auth, cross_tenant_workspace, missing_scope, or production_auth_required. Because checks fail closed, the absence of proof is itself a denial, and it stops the turn before anything is saved, any private context is loaded, or any provider tokens are spent. It is the locked door that stays locked by default, and the code on it explains why rather than leaving the caller guessing.",
  "host-context": "Browser page metadata such as the page origin, url, and title that the widget attaches to a request. It is reference data only: the server may use it for context, but it is never proof of identity or access, and it can never establish a tenant, workspace, user, role, or scope. Treat it like a return address written on an envelope, easy for anyone to write and never to be trusted as proof of who sent it.",
  "request-id": "A caller-chosen key that makes one submission safe to retry: if the same request id arrives twice, the server resolves it back to the turn already created instead of starting a second one. This is the system's only idempotency guarantee, so a dropped connection or an impatient retry costs nothing duplicate. It works like the confirmation number on an order: quote it again and you see the same order, not a second charge.",
  "assistant-turn-id": "The canonical handle for one assistant response once it has started, used to stream its events, check its status, and cancel it. Where the request id identifies the original submission, this id identifies the running turn itself, and every later interaction with that turn is keyed by it. It is the tracking number you follow after the order is placed, the one address all updates about that single reply flow through.",
  "branded-id": "A technique that tags a plain string or number as a specific kind of id at the type level, made through helpers like brandString and brandNumber. The point is that a raw, unbranded string simply will not compile where a tenant or workspace id is required, so whole classes of mix-ups, such as passing a user id where a workspace id belongs, are caught before the code ever runs. It is the difference between a loose key and one with an engraved label that only fits its own lock.",
  "partner-ai-service": "The deployable Service layer and the system's composition root: a Hono app that owns HTTP routes, SSE transport, config parsing, and the server-owned turn runner that forks generation onto a fiber keyed by assistantTurnId. It is the only place hono lives and the only thing you deploy, so it is where every port adapter gets wired together before any request runs. It is explicitly not a host app or a demo; this repo ships no production host app, and its mock and local capabilities fail closed outside explicit local profiles. It holds no product lifecycle or provider internals of its own, reaching those only through Core and the runtime port.",
  "partner-ai-core": "The Core layer, holding the product workflow, domain, policy, and ports, including the stream-chat workflow that prepares one turn and the step that maps each RuntimeEvent to a browser-facing sidechat.v1 event. It is where product decisions live, yet it stays deliberately thin on infrastructure: it never imports hono, a database driver, a provider SDK, React, or agent-runtime, reaching the runtime only through the AiRuntimePort contract. That isolation is what lets the provider, the persistence layer, and the transport all change without touching product logic.",
  "agent-runtime": "The Runtime layer, and the only place the AI SDK and provider code live. It runs one already-prepared turn: it calls the model, runs the tool loop, and emits normalized RuntimeEvents, owning no product policy or persistence, so the provider can be swapped without touching the rest of the system. Provider and AI-SDK stream parts never leak past its edge; they are converted to RuntimeEvents first. Because it sits at the innermost layer, everything else depends inward toward it through the ai-runtime-contract rather than the other way around.",
  "chat-protocol": "The browser-to-service contract, defining the sidechat.v1 request and event shapes, validators, sequence checks, the SSE codecs, and the error codes the widget and service both speak. It is one of the two contracts that cross a layer boundary, and it is intentionally dependency-light: it imports no React, hono, Effect, provider SDK, or other product package, so both the browser and the backend can depend on it without dragging in either side's machinery. Keeping the wire format in one shared package is what lets the widget and service evolve against a single source of truth.",
  "ai-runtime-contract": "The core-to-runtime contract, defining the provider-neutral AiRuntimeRequest, the RuntimeEvent union, the runtime port, and the branded ids that flow across that seam. It is the second of the two cross-boundary contracts and the only thing Core knows about the runtime, which is how Core stays free of any provider SDK while still driving model execution. Like the protocol package, it carries almost no dependencies, so a new runtime implementation can satisfy it without pulling in product or persistence code.",
  "side-chat-widget": "The Browser layer's embeddable React UI: the public widget API, a browser-safe API client, the SSE and activity readers, and the FSD-layered chat state. It is deliberately Effect-free and provider-free, depending only on chat-protocol, host-bridge, and shared, so it can never reach into Core, the runtime, or the database. It uses TanStack Query for the conversation list, history, and model catalog, but the live turn flows instead through an SSE reader, a module-level run store, and a pure reducer. Its state derives only from sidechat.v1 events and host-bridge messages, never from a RuntimeEvent or a database row.",
  "host-bridge": "The Browser-layer seam between the widget and the consuming host app, carrying host context and the host-command capability, dispatch, and result shapes. It is how the assistant asks the host to do something in the host's own UI, and the host returns a result, all in the browser rather than the runtime. It depends only on chat-protocol and shared, keeping it free of React and any backend concern, which preserves the clean line between host commands that run in the page and model-callable tools that run server-side.",
  "db": "The persistence package and the only home for the Postgres driver and Drizzle ORM. It owns the schema contract, the repository adapters, the durable turn_events log that is the source of truth for a turn, the lease operations, and the dedicated LISTEN/NOTIFY connection. Every database row stays behind its repository adapters, so no other layer ever sees a Drizzle row; the rest of the system reaches persistence only through ports. Holding the driver in one package is enforced by the boundary lints, which keep storage details from leaking into product, runtime, or widget code.",
  "chat-stream-request": "The browser-facing request that opens one sidechat.v1 turn, defined in chat-protocol and sent by the widget. It carries the user's content and may include a per-turn model preference, but it deliberately cannot carry provider options or set roles, because those are the server's to decide. It is the first shape in the inward request chain, and the service validates and translates it into the internal StreamChatInput before any product work begins.",
  "stream-chat-input": "The Core-layer input the service's HTTP adapter assembles from an incoming ChatStreamRequest, wrapping it with the resolved workspace, host app id, and the proven AuthContext. It is the hand-off shape at the service-to-core seam, marking the point where a raw browser request becomes something the stream-chat workflow can act on. Core checks authority on this value before touching context, persistence, or the model, so the boundary doubles as the gate where trust is established.",
  "ai-runtime-request": "The provider-neutral request Core passes into the runtime through the AiRuntimePort, the third shape in the inward chain. By the time it exists, Core has already assembled policy, trusted context, and history into its final messages, and it references tools only by name rather than carrying executables. It is what keeps Core ignorant of any provider: it describes what to run in neutral terms, leaving the runtime to turn names into real model and tool calls.",
  "runtime-provider-request": "The runtime-private request produced inside agent-runtime after it prepares a turn, the fourth and innermost shape in the chain. It takes the neutral AiRuntimeRequest, validates the requested tool names against the app-owned executable registrations, and replaces them with real RuntimeTool objects that must never leave the runtime. Executors receive this value, so it is the form of a turn that is finally ready to hit the provider.",
  "host-capability-manifest": "The host's declaration of everything a turn could possibly use: its available profiles, tools, commands, approval policies, and renderers. It is a statement of what exists, not a grant of access, and it lives in Core's capability contracts. The per-turn policy reads it to decide which of those names are actually allowed for a given turn, so the manifest is the menu while the policy is the order.",
  "turn-profile": "A versioned, named bundle of per-turn configuration that the policy selects for one turn, defined in Core's capability contracts. It packages the system instructions, the allowed tools and commands, and the related settings that shape how a turn behaves. Selecting a profile is how one host can offer several distinct assistant behaviors from the same service without changing code.",
  "tool-capability-vs-runtime-tool": "Two halves of the same tool kept deliberately apart: a ToolCapability is a manifest declaration that names a tool and is not executable, while a RuntimeTool is the app-owned code that actually runs, and it lives only inside agent-runtime. The split lets Core and the manifest reason about which tools may run without ever importing executable provider-side code. The two are bound together at composition time so a declared capability always has a real implementation behind it.",
  "service-tool-registration": "The composition record in the service that binds one ToolCapability to its matching RuntimeTool in a single registration. The registry asserts that the capability and the tool agree on the name, so the manifest and the runtime cannot drift apart over time. This bundled shape is also why declaring a tool capability without a working implementation is impossible: one registration must supply both halves.",
  "agent-executor": "The runtime engine inside agent-runtime that runs one fully prepared turn and emits only RuntimeEvents. By the time it runs, the model, messages, and tools have already been chosen, so its job is purely to drive execution, not to make product decisions. It is an injected port with a default identity of ai_sdk.tool_loop, which means an adopter can plug in a different execution strategy without disturbing anything outside the runtime.",
  "turn-guard": "A pre-context safety check in Core that inspects a turn before its context is assembled and returns allow, allow-with-warning, or block. It is the seam where an adopter enforces policy on whether a turn may proceed at all, ahead of any model work. Guards are injected ports that the service registers and the profile's safety policy then selects, so registering a guard is not the same as running it.",
  "approval-policy": "A policy, declared in Core's capability contracts, that requires explicit user or host approval before a particular declared tool or command is allowed to run. It is how a host gates sensitive or side-effectful actions behind a human or host decision rather than letting the model trigger them outright. It sits alongside the manifest and turn policy as part of how Core decides what a turn is permitted to do.",
  "copied-ui-primitive": "An external visual component vendored, rather than imported as a dependency, under the widget's shared/ai folder. This quarantine is a deliberate exception to the widget's no-provider-SDK rule: that folder alone may import the bare ai package, while the rest of the widget stays provider-free. Keeping these copies isolated lets the UI reuse upstream component code without letting provider machinery spread through the browser layer."
};
