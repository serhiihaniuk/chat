---
title: Glossary
description: One line each — the AI, architecture, and codebase terms used throughout the walkthrough, with where to grep.
---

One line each. When a term is taught in depth elsewhere, the page is linked. Three pages define
terms, each with a different job: [AI Vocabulary](/docs/walkthrough/vocabulary) is the plain-English
primer, [Confusing Words](/docs/walkthrough/confusing-words) disambiguates the overloaded codebase
nouns, and this glossary is the one-line index that points to both.

<Callout title="You will learn">
- Where to find a fast, one-line definition for every term in the walkthrough.
- How the terms group: AI/LLM words, architecture patterns, this-codebase nouns, transport/storage/UI, and TypeScript gotchas.
- Which deeper page owns each group, so you can jump from a definition to its full treatment.
</Callout>

## AI / LLM words

See [AI Vocabulary](/docs/walkthrough/vocabulary) for the full treatment.

| Term | One-line meaning |
| --- | --- |
| LLM | A stateless text-predictor. It remembers nothing between calls, so the server resends the whole transcript every turn. |
| Token | The chunk of text an LLM counts (~¾ of a word). You pay per token and every model has a hard limit. |
| Context window | The maximum tokens (history + new message + reply) a model accepts in one call. |
| Prompt | The text you send the model — in practice a list of messages, each tagged with a role. |
| Role | `system` (server's standing rules), `user` (the human), `assistant` (the model's past replies). The browser may set none of them. |
| Streaming / delta | Pushing the reply fragment-by-fragment as it is generated; each fragment is a delta. The UI concatenates them. |
| SSE | Server-Sent Events — a one-way HTTP stream (server → browser) carrying the deltas and activities. |
| Tool / function-calling | A function you let the model request. The model cannot act — it asks, your code runs it, you feed the result back. |
| Tool loop | Model asks → your code runs the tool → result fed back → model continues, repeating until it can answer. |
| Provider vs model | Provider = the API you call (only OpenAI is real here, plus a fake test provider). Model = an id like `gpt-5.4-mini`. |
| Reasoning | "Thinking" tokens a model spends privately before answering; hidden from the user by default. |
| Agentic | The system can take actions (call tools), observe results, and loop — not just reply once. |
| Turn | One user message in → one full assistant response out (including any tool loops). Recorded as one `assistant_turn` row. |

## Architecture & pattern words

See [Patterns in the Code](/docs/walkthrough/patterns).

| Term | One-line meaning |
| --- | --- |
| Port | An interface the core needs but does not implement (`ClockPort`, `AiRuntimePort`). |
| Adapter | A concrete object that fulfills a port using a real library (OpenAI, Postgres, HTTP). |
| Dependency inversion | Infrastructure depends on an interface the core owns — not the other way around. |
| Composition root | The one function (`composePartnerAiService`) that picks real adapters and wires them into the ports at startup. |
| Dependency injection | Passing a function what it needs as arguments instead of letting it reach for a global. |
| Factory | A `create*` function that turns typed config into one finished, validated bundle (e.g. `createServiceToolBundle`). |
| Strategy | A swappable behavior expressed as a function-shaped type and injected (the `SystemPromptBuilder`). |
| Registry | A boot-time validator that checks declarations and returns a frozen, name-indexed collection; fails loud on a bad name. |
| State machine | The stream's allowed transitions, so a wrong sequence is impossible — `protocol-stream-state-machine.ts` (`advanceProtocolStream`). |

## This-codebase nouns

See [Confusing Words](/docs/walkthrough/confusing-words) and [Add a Tool](/docs/walkthrough/add-a-tool).

| Term | One-line meaning | In the code |
| --- | --- | --- |
| Manifest | A deployment's declared menu of profiles/tools/commands. Data only — no executables, no secrets. Hashed per turn. | `HostCapabilityManifest` |
| Capability | One declared entry in the manifest — the description the model sees, never the running code. | `ToolCapability = { name, description, inputSchema }` |
| Registry | The boot-time object holding the real executables behind the manifest's declarations. | `createServiceToolRegistry` |
| Turn policy decision | The closed allowlist for one turn: model, profile, `allowedToolNames`, system instructions + manifest hash. | `TurnPolicyDecision` |
| Assistant profile | A named persona: system prompt + model + allowed tools + safety. Exactly one is active per turn. | `AssistantProfile` |
| Context candidate | A proposed piece of background (history, page info) that must survive a token budget before the model sees it. | `ContextCandidate` |
| Context board | The admitted context, rendered as one system message under a "not instructions" boundary with `Trust:`/`Source:` tags. | `render-context-board-message.ts` |
| Policy | A business-rule veto (quota, entitlement, model availability) — "may this caller spend a model call now?" | `PolicyPort.evaluate` |
| Guard | A pre-flight safety veto on the raw user text, before any prompt/tools are assembled. Fails closed. Noop by default. | `TurnGuardInput` |
| Repository | A port for reading/writing stored records, hiding the database behind named methods. | `ConversationRepositoryPort` |
| Noop adapter | An adapter that satisfies a required port by doing nothing on purpose — a placeholder. | `createNoopTurnGuardRegistry → { guards: [] }` |

## Transport, storage & UI words

| Term | One-line meaning |
| --- | --- |
| Turn-event registry | The owning instance's in-memory buffer of live `sidechat.v1` events. |
| Owner lease | The compare-and-swap claim (with a lease epoch + heartbeat) that lets exactly one instance own a running turn. |
| Reaper | The background sweep that terminalizes turns whose lease expired. |
| Registry sweep | Lazy cleanup that drops terminal, unwatched live buffers after retention. |
| `stream_unavailable` | HTTP 409: this instance does not own the running turn's live registry; poll status. |
| `replay_expired` | HTTP 404: the terminal live buffer is gone; read conversation history. |
| Host bridge | The seam that lets the widget ask the page it is embedded in to run an allowed command. |
| Drizzle | The TypeScript query-builder the Postgres repository uses. |
| FSD (feature-sliced design) | The widget's folder convention: shared → entities → features → widgets, lower layers never importing up. |

## TypeScript / library gotchas

| Term | One-line meaning |
| --- | --- |
| Effect | The async library used instead of `Promise`. `Effect.Effect<A, E>` = a lazy Promise of `A` that fails with typed `E`. |
| Stream | Effect's streaming type. `Stream.Stream<A, E>` = a sequence of `A`s that may fail with `E` (the protocol event stream). |
| Fiber | Effect's lightweight thread. Generation runs on a server-owned fiber that one instance owns and that outlives the HTTP request. |
| Branded type | `Brand<string, "RequestId">` — a plain string at runtime, with a compile-time label so ids cannot be mixed up. |
| Terminal event | The one event that closes a stream: `sidechat.completed`, `sidechat.error`, or `sidechat.blocked`. Nothing may follow it. |
| Finish reason | Why a completed turn ended: `stop` (normal), `length` (hit token limit), `aborted` (cancelled). |
