interface:
  display_name: "Side Chat Code Quality Gate"
  short_description: "Review code for safe human maintainability"
  default_prompt: "Use $side-chat-code-quality-gate to review this code change for readability, correctness, security boundaries, repository fit, and verification gaps."

policy:
  allow_implicit_invocation: true
