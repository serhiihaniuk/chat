# Optional agent metadata for the repository code quality gate skill.
# The canonical behavior lives in ../SKILL.md.
name: side-chat-code-quality-gate
version: 1.3.0
triggers:
  - repository code quality
  - code smells
  - cognitive complexity
  - human cognitive load
  - clever hard to understand code
  - comments assume too much context
  - AI SDK stream readability
  - AI SDK ToolLoopAgent
  - Oxlint Oxfmt TypeScript custom lints
  - package boundaries
  - widget layers
  - runtime boundaries
